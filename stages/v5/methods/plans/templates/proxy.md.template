# Proxy — {tunnel_id}

## Purpose
- Fixed intrinsic proxies used as deployment-time reflection triggers.
- These scores are not trained mIoU predictors.

## Score definitions
- **S_depth = S_coverage * S_empty**
- **S_boundary = S_continuity * S_K * S_spacing * S_layout**

All components are normalized to **[0, 1]**, where higher is better.

## Validation split
- **Threshold-selection trials:** (list trial IDs or rule)
- **Held-out trials:** (list or rule; not used for threshold choice)

## Spearman sanity check
- **S_depth vs final mIoU:** 
- **S_boundary vs final mIoU:** 

## Failure event and thresholds
- **tau** (bad final result threshold): e.g. `mIoU < 0.60`
- **T_depth:** 
- **T_boundary:** 

## Trigger validation
- **Bad-case recall:** 
- **Trigger precision:** 
- **False-negative rate:** 
- **Accepted-case mIoU:** 

## Deployment rule
- Trigger preprocessing reflection when **S_depth < T_depth**.
- Trigger detection/boundary reflection when **S_boundary < T_boundary**.
- Accept when neither trigger fires.

## Optional appendix only
- Leave-one-out ablation:
- Ridge regression:
- Logistic/Platt calibration:
