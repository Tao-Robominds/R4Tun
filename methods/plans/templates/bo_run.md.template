# BO Run — {tunnel_id} / {run_id}

## Metadata
- Tunnel: {tunnel_id}
- Run: {run_id}
- Ring: {ring_id}
- Stage: (preprocessing | detection_boundary)
- Objective: (stage reward; final mIoU logged for validation)

## Search space
- Params: (list from warm_start / critical_params)
- Bounds: (min/max per param)
- Priors: (optional)

## Logging contract
- Per eval: run.json, stage_manifest.json, reflection_log.json
- Sandbox path: logs/{run_id}/{tunnel_id}/r{ring_id}/02_bo_calibration/

## Stop rule
- (e.g. max trials, convergence, time)

## Intrinsic proxy diagnostics
- Log the raw components needed for `S_depth` or `S_boundary`.
- Fixed proxy thresholds are selected later; BO should not fit a proxy model here.
