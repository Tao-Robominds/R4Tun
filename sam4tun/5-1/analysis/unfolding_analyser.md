# Unfolding Analysis Recommendations - 5-1

---


{"tunnel_id": "5-1", "recommended_changes": [{"line": "delta = 0.005", "new_value": "delta = 0.01", "reason": "Increase slice thickness to match new average point spacing (~0.009 m) and ensure sufficient points per slice."}, {"line": "n = round(l / 1.2)", "new_value": "n = round(l / 1.0)", "reason": "Use 1.0 m slice spacing instead of 1.2 m to generate more rings over the 12.67 m tunnel length for finer centre‐line resolution."}, {"line": "filtered_points = [point for point in points if abs(point[1] - y_max) <= 4.5]", "new_value": "filtered_points = [point for point in points if abs(point[1] - y_max) <= 6.0]", "reason": "Expand the vertical filter window to ±6 m to accommodate the increased tunnel height (≈7.57 m)."}, {"line": "ransac = RANSAC(data=points_data, threshold=1.0, P=0.9, S=0.75, N=5)", "new_value": "ransac = RANSAC(data=points_data, threshold=1.5, P=0.9, S=0.75, N=5)", "reason": "Relax the inlier threshold to 1.5 m for ellipse fitting due to increased radius and point‐cloud variability."}, {"line": "degree = 3", "new_value": "degree = 2", "reason": "Lower polynomial degree to prevent overfitting on the smoother curvature of the larger tunnel."}], "justification": "The new tunnel has a larger diameter, greater height, and lower point density, so we increase slice thickness and filter window size, adjust slice spacing for more rings, relax RANSAC tolerance, and reduce polynomial degree to achieve robust centre‐line extraction."}